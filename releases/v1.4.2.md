# v1.4.2

## Preserve home-directory notation in config

### Fixed

- Preserved `~/` notation when saving `.ai-jail` files (#52). Paths under the user's home directory still expand for execution, but round-trip back to a portable `~/...` form when config is written.
