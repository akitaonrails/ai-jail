# v1.5.0

## OSC 52 clipboard forwarding

### Fixed

- Forwarded OSC 52 clipboard writes through the alternate-screen PTY proxy (#55). This lets terminal clipboard operations keep working while ai-jail's status-bar PTY proxy is active.
