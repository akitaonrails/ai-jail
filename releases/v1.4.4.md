# v1.4.4

## Relative map paths

### Fixed

- Resolved relative `--map` / `--rw-map` paths against the project directory before passing them to bwrap and Landlock (#54). This allows portable parent-directory paths such as `--map ../shared-data` in multi-project repositories.
