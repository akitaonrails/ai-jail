# v1.4.3

## Global Git settings passthrough

### Added

- Mounted the user's global Git ignore file read-only inside the sandbox.
- Mounted Git's XDG global settings directory read-only inside the sandbox.
- Honored `$XDG_CONFIG_HOME` when locating the global Git settings directory, falling back to `~/.config/git` when unset.
